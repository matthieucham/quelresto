var React = require('react/addons');
var Page = React.createClass({
  /** @cfg {object} Panel configuration */
  panelConfig: {
    header: {
      bgColor: 'bg-orange75',
      labelMainTitle: 'test'
    }
  },

  render: function () {
    return false;
  }
});

module.exports = Page;
